# Echoes-of-Babylon


## To run my script:

```python
pip install -r requirements.txt

cp .env.example .env

python src/prepare_data.py

python src/train.py
```
You can run it in GPU enviroment only (I guess)
### 1) It Installs all requirements
### 2) Copies (cp) an env example file for you to fill in
### 3) Splits and prepares the data
### 4) Trains a model in output folder